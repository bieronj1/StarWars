#include <iostream>
#include <string>
#include <SFML/Graphics.hpp>
#include <SFML/System.hpp>
#include <math.h>
#include <cmath>

class Weapon{
  public:
		int type;
		float cd = 0;
		float cdtime;
		float dmg;
		float spd;
		float rad;
		
		
		Weapon(int xtype, float xcd, float xdmg, float xspd, float xrad);
		void updateCD();
		bool fire();
		

	
	};
